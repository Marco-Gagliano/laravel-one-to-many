<?php

namespace App;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Str;

class Category extends Model
{
    // $data =  ['HTML', 'CSS', 'Javascript', 'PHP', 'C++', 'Bootstrap', 'Laravel'];

    // foreach ($data as $key => $value) {

    // }
}
